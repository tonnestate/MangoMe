# Contributing

Preserve the central invariants: workers make claims, MangoMe derives state; plans precede productive mutation; contract contributions are append-only; collisions warn rather than lock; ambiguous discovery never becomes canonical truth automatically.

Run `pytest` before submitting changes.
